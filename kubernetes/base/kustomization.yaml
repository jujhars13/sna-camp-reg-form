apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gurdwara-displays

labels:
- includeSelectors: true
  pairs:
    projectName: gurdwara-displays

resources:
  - namespace.yaml
  - sa.yaml
  - pvc.yaml
  - backend.yaml
  - frontend.yaml
  - ingress.yaml
